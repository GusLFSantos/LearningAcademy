<% content_for :title, "New Lesson" %>
<div class="max-w-3xl mx-auto">
  <%= link_to "← #{@course.title}", course_path(@course), class: "text-sm text-gray-500 hover:text-indigo-600" %>
  <h1 class="text-2xl font-bold mt-3 mb-6">New Lesson</h1>
  <%= render "form", course: @course, lesson: @lesson %>
</div>
